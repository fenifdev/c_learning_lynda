#include <stdio.h>
#include <math.h>

/*int main()
{
    int a;
    int b = 5;

    printf("Input an integer: ");
    scanf("%d", &a);
    printf("%d + %d = %d\n", a, b, a+b);

    return(0);
}*/

/*int main()
{
    float r;

    r = sqrt(2.0);

    printf("The square root of 2 is %f\n", r);

    return(0);
}*/

int main()
{
    float p;

    p = pow(2.0, 8.0);

    printf("2 to the 8th power is %f\n", p);

    return(0);
}
